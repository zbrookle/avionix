.. include:: ../../shared/links.rst
.. _inheritance:

Inheritance
===========

The real power of :mod:`avionix` comes from it being object-oriented and written in a
programming language as opposed to the limited logic that comes with yaml. One aspect
of programming that is missing from helm_ that :mod:`avionix` has is inheritance.
This is an excerpt from another package built with :mod:`avionix`, avionix_airflow_

.. literalinclude:: ../../../examples/inheritance_example.py
   :language: python
